// Example of a simple progress bar update (can be expanded)
function updateProgressBar(progress) {
    document.getElementById('progress').style.width = progress + '%';
    document.getElementById('progress-text').innerText = progress + '%';
}
